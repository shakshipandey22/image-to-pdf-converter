# image-to-pdf-converter
A Python desktop application built with Tkinter and Pillow that allows users to select multiple images (JPG, PNG, BMP) and convert them into a single PDF file. The app automatically converts images to RGB format for compatibility and provides a simple, user-friendly interface.
